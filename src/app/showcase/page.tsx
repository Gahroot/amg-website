import { ShowcaseClient } from "@/components/sections/showcase/showcase-client";

export const metadata = {
  title: "Domains Showcase",
  description:
    "Three design options for the AMG capability domains visualization.",
};

export default function ShowcasePage() {
  return <ShowcaseClient />;
}
